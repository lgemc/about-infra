apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gogs-db-claim
spec:
  storageClassName: ""
  volumeName: gogs-db
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
