export enum ContextValues {
  RequestID = "request_id",
  ClientID = "client_id",
  Company = "company",
  // Source tells the place where context comes
  Source = "source",
  // Environment tells if context was originated from staging, production or development env
  Environment = "environment",
  UserID = "user_id",
  RoutineID = "routine_id",
}
